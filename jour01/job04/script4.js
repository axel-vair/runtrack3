
function bisextile(year) { // on fait un modulo 4 de l'année entrée et on return true si c'est une année bisextile, false si ce n'est pas le cas
    console.debug(year % 4 == 0)
    return (year % 4 == 0);
}
